{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "builds": [
    { "src": "/api/(.*)", "use": "@vercel/python", "config": {} }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/$1" }
  ]
}